#include <stdio.h>
/**
 * main - Entry point
 *
 * Description A C program print from 1 to 9
 *
 *  Return: Always 0 (Success)
 */
int main(void)
{
	int c = 0;

	while (c <= 9)
	{
		putchar(c + 48)
		if (digit != 9)
		{
			putchar(',');
			putchar(' ');
		}
		c++;
	}
	putchar('\n');
	return (0);
}
